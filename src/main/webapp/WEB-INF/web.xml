<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    
    <session-config>
        <!-- The session-timeout element defines the default session timeout interval 
             for all sessions created in this web application. The timeout is specified 
             in minutes. -->
        <session-timeout>15</session-timeout>
    </session-config>
    
    <!--
      The "default" servlet is responsible for serving static resources.
      The following mappings tell the container to use the default servlet
      for requests to these common static content directories.
    -->
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/css/*</url-pattern>
        <url-pattern>/js/*</url-pattern>
        <url-pattern>/images/*</url-pattern>
        <url-pattern>/uploads/*</url-pattern>
    </servlet-mapping>

</web-app>